# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Use a package of configuration called an orb.
orbs:
  # Declare a dependency on the welcome-orb
  welcome: circleci/welcome-orb@0.4.1
# Orchestrate or schedule a set of jobs
jobs:
  save_hello_world_output:
    docker:
    - image: circleci/node:13.8.0
    steps:
    - run:
        name: save_hello_world
        command: echo "hello world" > /tmp/save_hello_world_output/output.txt
    - persist_to_workspace:
        root: /tmp/save_hello_world_output
        paths:
        - output.txt
  print_output_file:
    docker:
    - image: circleci/node:13.8.0
    steps:
    - attach_workspace:
        at: /tmp/save_hello_world_output
    - run:
        name: print_output
        command: cat /tmp/save_hello_world_output/output.txt



    
  # print-hello:
  #   docker:
  #   - image: circleci/node:13.8.0
  #   steps:
  #   - run:
  #       name: echo hello
  #       command: echo HELLO
  # print-world:
  #   docker:
  #   - image: circleci/node:13.8.0
  #   steps:
  #   - run:
  #       name: echo world
  #       command: echo WORLD
  # print-name:
  #   docker:
  #   - image: circleci/node:13.8.0
  #   steps:
  #   - run:
  #       name: echo name
  #       command: echo ${UDACITY_NAME} | base64


workflows:
  # Name the workflow "welcome"
  welcome:
    # Run the welcome/run job in its own container
    jobs:
      - save_hello_world_output
      - print_output_file:
          requires:
          - save_hello_world_output
      # - welcome/run
      # - print-hello
      # - print-world:
      #     requires: [print-hello]
      # - print-name:
      #     requires: 
      #     - print-world
          
